#include <bits/stdc++.h>
#define fi first
#define se second
#define all(x) (x).begin(), (x).end()
typedef long long ll;

using namespace std;

int main() {
  int A, B, C;
  cin >> A >> B >> C;
  int sm = 0;
  // sum must be multiple of A, and the difference of the sum and C must be a multiple of B; that is, sum - C % B == 0 must be true
}